name: apk

on:
  push:
    branches: ["main"]
    paths:
      - "**/*.dart"
      - "pubspec.yaml"
      - "**/dart.yml"

jobs:
  build:
    if: "contains(github.event.head_commit.message, 'apk')"
    runs-on: ubuntu-latest
    steps:
      - name: Output Commit Information
        run: |
          echo "触发此次action的提交ID: ${{ github.sha }}"
          echo "提交信息: ${{ github.event.head_commit.message }}"
          echo "提交作者: ${{ github.event.head_commit.author.name }}"
          GITHASH=$(echo "${{ github.sha }}" | cut -c1-7)
          echo "GITHASH=$GITHASH" >> $GITHUB_ENV
          echo "GITHASH (前7位): $GITHASH"
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "21"
      - uses: subosito/flutter-action@v2
        with:
          channel: "stable"
          flutter-version: "3.38.10"
      - run: flutter --version
      - run: flutter pub get 

      - name: 解码生成 jks
        run: echo $KEYSTORE_BASE64 | base64 -di > android/app/xuan.jks
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
      # - run: cat android/app/xuan.jks

      # ============ 轮1: 带 FFmpeg + 默认 Cronet ============
      - name: Switch to FFmpeg-enabled config
        run: bash scripts/build.sh with

      - run: flutter pub get

      - name: flutter build apk --split-per-abi (ffmpeg)
        run: flutter build apk --release --split-per-abi --dart-define=gitHash=${{ env.GITHASH }}
        env:
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
      - name: flutter build apk (ffmpeg)
        run: flutter build apk --release --dart-define=gitHash=${{ env.GITHASH }}
        env:
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
          
      - name: Rename APK files (ffmpeg, default cronet)
        run: |
          mv build/app/outputs/flutter-apk/app-arm64-v8a-release.apk "build/app/outputs/flutter-apk/app-arm64-v8a-release-without embedded Cronet-ffmpeg-${GITHASH}.apk"
          mv build/app/outputs/flutter-apk/app-armeabi-v7a-release.apk "build/app/outputs/flutter-apk/app-armeabi-v7a-release-without embedded Cronet-ffmpeg-${GITHASH}.apk"
          mv build/app/outputs/flutter-apk/app-x86_64-release.apk "build/app/outputs/flutter-apk/app-x86_64-release-without embedded Cronet-ffmpeg-${GITHASH}.apk"
          mv build/app/outputs/flutter-apk/app-release.apk "build/app/outputs/flutter-apk/app-release-without embedded Cronet-ffmpeg-${GITHASH}.apk"
        shell: bash
          
        # 上传分平台生成的 APK 文件

      - name: Upload arm64-v8a APK (ffmpeg)
        uses: actions/upload-artifact@v4
        with:
          name: app-arm64-v8a-release-without embedded Cronet-ffmpeg.apk
          path: build/app/outputs/flutter-apk/app-arm64-v8a-release-without embedded Cronet-ffmpeg-${{ env.GITHASH }}.apk
      - name: Upload armeabi-v7a APK (ffmpeg)
        uses: actions/upload-artifact@v4
        with:
          name: app-armeabi-v7a-release-without embedded Cronet-ffmpeg.apk
          path: build/app/outputs/flutter-apk/app-armeabi-v7a-release-without embedded Cronet-ffmpeg-${{ env.GITHASH }}.apk
      - name: Upload x86_64 APK (ffmpeg)
        uses: actions/upload-artifact@v4
        with:
          name: app-x86_64-release-without embedded Cronet-ffmpeg.apk
          path: build/app/outputs/flutter-apk/app-x86_64-release-without embedded Cronet-ffmpeg-${{ env.GITHASH }}.apk

      - name: Upload APK (ffmpeg)
        uses: actions/upload-artifact@v4
        with:
          name: app-release-without embedded Cronet-ffmpeg.apk
          path: build/app/outputs/flutter-apk/app-release-without embedded Cronet-ffmpeg-${{ env.GITHASH }}.apk

      # ============ 轮2: 带 FFmpeg + cronetHttpNoPlay ============
      - name: flutter build apk --split-per-abi --dart-define=cronetHttpNoPlay=true (ffmpeg)
        run: flutter build apk --release --split-per-abi --dart-define=cronetHttpNoPlay=true --dart-define=gitHash=${{ env.GITHASH }}
        env:
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}

      - name: flutter build apk (ffmpeg, cronetHttpNoPlay)
        run: flutter build apk --release --dart-define=cronetHttpNoPlay=true --dart-define=gitHash=${{ env.GITHASH }}
        env:
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
      - name: Rename APK files (ffmpeg, cronetHttpNoPlay)
        run: |
          mv build/app/outputs/flutter-apk/app-arm64-v8a-release.apk "build/app/outputs/flutter-apk/app-arm64-v8a-release-ffmpeg-${GITHASH}.apk"
          mv build/app/outputs/flutter-apk/app-armeabi-v7a-release.apk "build/app/outputs/flutter-apk/app-armeabi-v7a-release-ffmpeg-${GITHASH}.apk"
          mv build/app/outputs/flutter-apk/app-x86_64-release.apk "build/app/outputs/flutter-apk/app-x86_64-release-ffmpeg-${GITHASH}.apk"
          mv build/app/outputs/flutter-apk/app-release.apk "build/app/outputs/flutter-apk/app-release-ffmpeg-${GITHASH}.apk"
        shell: bash
        # 上传分平台生成的 APK 文件
      - name: Upload arm64-v8a APK (ffmpeg, cronetHttpNoPlay)
        uses: actions/upload-artifact@v4
        with:
          name: app-arm64-v8a-release-ffmpeg.apk
          path: build/app/outputs/flutter-apk/app-arm64-v8a-release-ffmpeg-${{ env.GITHASH }}.apk
      - name: Upload armeabi-v7a APK (ffmpeg, cronetHttpNoPlay)
        uses: actions/upload-artifact@v4
        with:
          name: app-armeabi-v7a-release-ffmpeg.apk
          path: build/app/outputs/flutter-apk/app-armeabi-v7a-release-ffmpeg-${{ env.GITHASH }}.apk
      - name: Upload x86_64 APK (ffmpeg, cronetHttpNoPlay)
        uses: actions/upload-artifact@v4
        with:
          name: app-x86_64-release-ffmpeg.apk
          path: build/app/outputs/flutter-apk/app-x86_64-release-ffmpeg-${{ env.GITHASH }}.apk

      - name: Upload APK (ffmpeg, cronetHttpNoPlay)
        uses: actions/upload-artifact@v4
        with:
          name: app-release-ffmpeg.apk
          path: build/app/outputs/flutter-apk/app-release-ffmpeg-${{ env.GITHASH }}.apk

      # ============ 轮3: 无 FFmpeg 精简版 + 默认 Cronet ============
      - name: Switch to FFmpeg-free config
        run: bash scripts/build.sh without

      - run: flutter pub get

      - name: flutter build apk --split-per-abi (lite)
        run: flutter build apk --release --split-per-abi --dart-define=gitHash=${{ env.GITHASH }}
        env:
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
      - name: flutter build apk (lite)
        run: flutter build apk --release --dart-define=gitHash=${{ env.GITHASH }}
        env:
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
          
      - name: Rename APK files (lite, default cronet)
        run: |
          mv build/app/outputs/flutter-apk/app-arm64-v8a-release.apk "build/app/outputs/flutter-apk/app-arm64-v8a-release-without embedded Cronet-${GITHASH}.apk"
          mv build/app/outputs/flutter-apk/app-armeabi-v7a-release.apk "build/app/outputs/flutter-apk/app-armeabi-v7a-release-without embedded Cronet-${GITHASH}.apk"
          mv build/app/outputs/flutter-apk/app-x86_64-release.apk "build/app/outputs/flutter-apk/app-x86_64-release-without embedded Cronet-${GITHASH}.apk"
          mv build/app/outputs/flutter-apk/app-release.apk "build/app/outputs/flutter-apk/app-release-without embedded Cronet-${GITHASH}.apk"
        shell: bash
          
        # 上传分平台生成的 APK 文件

      - name: Upload arm64-v8a APK (lite)
        uses: actions/upload-artifact@v4
        with:
          name: app-arm64-v8a-release-without embedded Cronet.apk
          path: build/app/outputs/flutter-apk/app-arm64-v8a-release-without embedded Cronet-${{ env.GITHASH }}.apk
      - name: Upload armeabi-v7a APK (lite)
        uses: actions/upload-artifact@v4
        with:
          name: app-armeabi-v7a-release-without embedded Cronet.apk
          path: build/app/outputs/flutter-apk/app-armeabi-v7a-release-without embedded Cronet-${{ env.GITHASH }}.apk
      - name: Upload x86_64 APK (lite)
        uses: actions/upload-artifact@v4
        with:
          name: app-x86_64-release-without embedded Cronet.apk
          path: build/app/outputs/flutter-apk/app-x86_64-release-without embedded Cronet-${{ env.GITHASH }}.apk

      - name: Upload APK (lite)
        uses: actions/upload-artifact@v4
        with:
          name: app-release-without embedded Cronet.apk
          path: build/app/outputs/flutter-apk/app-release-without embedded Cronet-${{ env.GITHASH }}.apk

      # ============ 轮4: 无 FFmpeg 精简版 + cronetHttpNoPlay ============
      - name: flutter build apk --split-per-abi --dart-define=cronetHttpNoPlay=true (lite)
        run: flutter build apk --release --split-per-abi --dart-define=cronetHttpNoPlay=true --dart-define=gitHash=${{ env.GITHASH }}
        env:
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}

      - name: flutter build apk (lite, cronetHttpNoPlay)
        run: flutter build apk --release --dart-define=cronetHttpNoPlay=true --dart-define=gitHash=${{ env.GITHASH }}
        env:
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
      - name: Rename APK files (lite, cronetHttpNoPlay)
        run: |
          mv build/app/outputs/flutter-apk/app-arm64-v8a-release.apk "build/app/outputs/flutter-apk/app-arm64-v8a-release-${GITHASH}.apk"
          mv build/app/outputs/flutter-apk/app-armeabi-v7a-release.apk "build/app/outputs/flutter-apk/app-armeabi-v7a-release-${GITHASH}.apk"
          mv build/app/outputs/flutter-apk/app-x86_64-release.apk "build/app/outputs/flutter-apk/app-x86_64-release-${GITHASH}.apk"
          mv build/app/outputs/flutter-apk/app-release.apk "build/app/outputs/flutter-apk/app-release-${GITHASH}.apk"
        shell: bash
        # 上传分平台生成的 APK 文件
      - name: Upload arm64-v8a APK (lite, cronetHttpNoPlay)
        uses: actions/upload-artifact@v4
        with:
          name: app-arm64-v8a-release.apk
          path: build/app/outputs/flutter-apk/app-arm64-v8a-release-${{ env.GITHASH }}.apk
      - name: Upload armeabi-v7a APK (lite, cronetHttpNoPlay)
        uses: actions/upload-artifact@v4
        with:
          name: app-armeabi-v7a-release.apk
          path: build/app/outputs/flutter-apk/app-armeabi-v7a-release-${{ env.GITHASH }}.apk
      - name: Upload x86_64 APK (lite, cronetHttpNoPlay)
        uses: actions/upload-artifact@v4
        with:
          name: app-x86_64-release.apk
          path: build/app/outputs/flutter-apk/app-x86_64-release-${{ env.GITHASH }}.apk

      - name: Upload APK (lite, cronetHttpNoPlay)
        uses: actions/upload-artifact@v4
        with:
          name: app-release.apk
          path: build/app/outputs/flutter-apk/app-release-${{ env.GITHASH }}.apk

      # 从 pubspec.yaml 提取版本号
      - name: Extract Version from pubspec.yaml
        id: extract_version
        run: |
          $version = (Select-String -Path pubspec.yaml -Pattern '^version:' | ForEach-Object { $_.Line -replace 'version:\s*', '' }).Trim()
          echo "VERSION=$version" >> $env:GITHUB_ENV
        shell: pwsh
      - name: Debug Version
        run: echo "${{ env.VERSION }}"
      
      # 删除该 Release 下所有旧的 APK 文件
      - name: Delete Old APK Assets from Release
        run: |
          gh release view "${{ env.VERSION }}" --json assets --jq '.assets[].name' | grep -E '\.apk$' | while read asset; do
            echo "Deleting old asset: $asset"
            gh release delete-asset "${{ env.VERSION }}" "$asset" --yes || true
          done
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        continue-on-error: true
        
      # 上传新的资产到最新 Release（ffmpeg 版 + 精简版，各 8 个）
      - name: Upload New Asset to Release
        uses: softprops/action-gh-release@v2.3.2
        with:
          tag_name: ${{ env.VERSION }}
          prerelease: ${{ !contains(github.event.head_commit.message, 'sta') }}
          files: |
            build/app/outputs/flutter-apk/app-release-without embedded Cronet-ffmpeg-${{ env.GITHASH }}.apk
            build/app/outputs/flutter-apk/app-arm64-v8a-release-without embedded Cronet-ffmpeg-${{ env.GITHASH }}.apk
            build/app/outputs/flutter-apk/app-armeabi-v7a-release-without embedded Cronet-ffmpeg-${{ env.GITHASH }}.apk
            build/app/outputs/flutter-apk/app-x86_64-release-without embedded Cronet-ffmpeg-${{ env.GITHASH }}.apk
            build/app/outputs/flutter-apk/app-release-ffmpeg-${{ env.GITHASH }}.apk
            build/app/outputs/flutter-apk/app-arm64-v8a-release-ffmpeg-${{ env.GITHASH }}.apk
            build/app/outputs/flutter-apk/app-armeabi-v7a-release-ffmpeg-${{ env.GITHASH }}.apk
            build/app/outputs/flutter-apk/app-x86_64-release-ffmpeg-${{ env.GITHASH }}.apk
            build/app/outputs/flutter-apk/app-release-without embedded Cronet-${{ env.GITHASH }}.apk
            build/app/outputs/flutter-apk/app-arm64-v8a-release-without embedded Cronet-${{ env.GITHASH }}.apk
            build/app/outputs/flutter-apk/app-armeabi-v7a-release-without embedded Cronet-${{ env.GITHASH }}.apk
            build/app/outputs/flutter-apk/app-x86_64-release-without embedded Cronet-${{ env.GITHASH }}.apk
            build/app/outputs/flutter-apk/app-release-${{ env.GITHASH }}.apk
            build/app/outputs/flutter-apk/app-arm64-v8a-release-${{ env.GITHASH }}.apk
            build/app/outputs/flutter-apk/app-armeabi-v7a-release-${{ env.GITHASH }}.apk
            build/app/outputs/flutter-apk/app-x86_64-release-${{ env.GITHASH }}.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
